app:
  host: ${HOST}
  port: ${PORT}
  secure: ${SECURE}

database:
  influx:
    host: ${INFLUXDB_HOST}
    port: ${INFLUXDB_PORT}

currency:
  currencyconverterapi:
    apiKey: ${CURRENCYCONVERTERAPI_API_KEY}
